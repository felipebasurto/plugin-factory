# plugin-factory (Codex package)

Codex installs this folder via `.agents/plugins/marketplace.json` at the repo root.

Claude and Cursor use the **repository root** as the plugin directory instead.

Content is synced from the root with `../../scripts/sync-codex-plugin.sh` at release time.
